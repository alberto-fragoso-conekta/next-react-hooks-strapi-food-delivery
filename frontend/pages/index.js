import { Button, Alert } from 'reactstrap'

const Home = () => (
  <div>
    <Alert color='primary'>
      Jelo ma frend from Next.js.
    </Alert>
    <Button color='primary'>Oh ray!</Button>
  </div>
)

export default Home